(library
 (name libcervino)  
 (libraries containers menhirLib sexplib stdlib-shims) 
 (preprocess (pps ppx_deriving ppx_sexp_conv)))

(menhir
  (modules parser)
  (flags --explain --dump)
  (infer true)
)

(ocamllex scanner)