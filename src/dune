(library
 (name libcervino)
 (libraries containers fmt hashcons logs logs.fmt)
  (preprocess (pps ppx_deriving.make ppx_deriving.show)) )

(menhir
 (modules parser)
 (infer true))

(ocamllex scanner)
